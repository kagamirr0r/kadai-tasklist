<%= form_for(task) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>
  <br>
  <%= f.label :status, 'ステータス'%>
  <%= f.text_field :status %>

  <%= f.submit '追加' %>
<% end %>

<%= link_to '一覧に戻る', tasks_path %>